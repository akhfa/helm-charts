apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "lobe-chat.fullname" . }}-test-connection"
  labels:
    {{- include "lobe-chat.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "lobe-chat.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
